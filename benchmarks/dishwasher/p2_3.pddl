(define (problem p_2_3)
  (:domain dishwasher)
  (:objects d1 d2 d3 - dish r1 r2 - room)
  (:init (can-arrive d1)
    (can-arrive d2)
    (can-arrive d3))
  (:goal (and (clean d1)
    (clean d2)
    (clean d3)))
)